!!!
%html
  %head
    %meta{:charset => "utf-8"}
    %title= @item[:title]
    %link{:href => "/style.css", :media => "screen", :rel => "stylesheet", :type => "text/css"}
    %meta{:content => "nanoc 3.1.7", :name => "generator"}
  %body
    #container
      #head 
        %h1 
          %a{:href => "/"}Nanoc Starter
      #topbar
        .topbar 
          %a{:href => "/branch/"}Branch
        .topbar 
          %a{:href => "/contact/"}Contact
      -# layout is chosen in the content file using the 'layout' attribute
      -# pass 'yield' into the chosen partial 'layout'
      = render @item[:layout], :content => yield 
      = render "footer"
      -# = render "bottom"
